---
phase: 08-dashboard-analytics
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/package.json
  - apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx
  - apps/web/src/components/analytics/team-overview.tsx
  - apps/web/src/components/analytics/summary-card.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-05]

must_haves:
  truths:
    - "Owner navigates to /manager/{teamId}/analytics and sees the Team Overview tab by default"
    - "4 summary KPI cards show submission rate %, avg hours/day, stress distribution, and task count with trend arrows"
    - "Submission Rate line chart shows daily percentages over the selected period"
    - "Workload Heatmap shows members x days grid color-coded by hours (green/yellow/red)"
    - "Stress Trend stacked area chart shows Low/Medium/High distribution over time"
    - "Task Volume horizontal bar chart shows tasks ranked by member"
    - "Week/Month/Quarter toggle changes the time range and all charts update"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      provides: "Analytics page with tab navigation and time range toggle"
      min_lines: 40
    - path: "apps/web/src/components/analytics/team-overview.tsx"
      provides: "Team Overview tab with 4 summary cards and 4 charts"
      min_lines: 100
    - path: "apps/web/src/components/analytics/summary-card.tsx"
      provides: "Reusable KPI summary card with trend arrow"
      min_lines: 20
  key_links:
    - from: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      to: "/api analytics endpoints"
      via: "api.get with useQuery"
      pattern: "api\\.get.*analytics/team"
    - from: "apps/web/src/components/analytics/team-overview.tsx"
      to: "recharts"
      via: "LineChart, AreaChart, BarChart components"
      pattern: "from 'recharts'"
---

<objective>
Build the analytics page shell (with tab navigation and time range toggle) and the Team Overview tab with 4 summary KPI cards and 4 charts (submission rate line chart, workload heatmap, stress trend stacked area, task volume horizontal bar chart). Install Recharts as the charting library.

Purpose: Give team owners a visual dashboard to spot team trends at a glance -- who is submitting, who is overloaded, how stress is trending.
Output: Analytics page at /manager/{teamId}/analytics with fully functional Team Overview tab.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-dashboard-analytics/08-CONTEXT.md
@.planning/phases/08-dashboard-analytics/08-01-SUMMARY.md

@apps/web/src/app/(dashboard)/manager/[teamId]/page.tsx
@apps/web/src/lib/api-client.ts
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/button.tsx

<interfaces>
<!-- Types from 08-01 that this plan consumes -->

From packages/shared/src/types/analytics.ts:
```typescript
export type AnalyticsRange = 'week' | 'month' | 'quarter';

export interface TeamAnalyticsSummary {
  submissionRate: number;
  submissionRateTrend: number;
  avgHoursPerDay: number;
  avgHoursPerDayTrend: number;
  stressDistribution: { low: number; medium: number; high: number };
  taskCount: number;
  taskCountTrend: number;
}

export interface DailySubmissionRate { date: string; rate: number; submitted: number; total: number; }
export interface HeatmapCell { userId: string; displayName: string; date: string; hours: number; }
export interface DailyStressDistribution { date: string; low: number; medium: number; high: number; }
export interface MemberTaskVolume { userId: string; displayName: string; taskCount: number; }

export interface TeamAnalyticsResponse {
  range: AnalyticsRange;
  startDate: string;
  endDate: string;
  summary: TeamAnalyticsSummary;
  submissionRates: DailySubmissionRate[];
  heatmap: HeatmapCell[];
  stressTrend: DailyStressDistribution[];
  taskVolumeByMember: MemberTaskVolume[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and create analytics page shell with summary card component</name>
  <files>apps/web/package.json, apps/web/src/components/analytics/summary-card.tsx, apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx</files>
  <action>
1. Install recharts in the web workspace:
   `cd /Users/avepoint_admin/Downloads/daily-report-webapp && pnpm add recharts --filter=web`

2. Create `apps/web/src/components/analytics/summary-card.tsx`:
   - Reusable `SummaryCard` component accepting props: `{ title: string; value: string | number; trend?: number; trendLabel?: string; subtitle?: string; children?: ReactNode }`
   - Uses the existing Card/CardHeader/CardContent from `@/components/ui/card`
   - Trend arrow: green up arrow if trend > 0, red down arrow if trend < 0, gray dash if 0 or undefined
   - Format trend as "+X%" or "-X%" next to the arrow
   - `children` slot for custom content (used by stress distribution card which shows a mini bar instead of a single number)

3. Create `apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx`:
   - 'use client' page component
   - Use `useParams()` to get teamId
   - State: `activeTab: 'team' | 'member'` (default: 'team'), `range: AnalyticsRange` (default: 'week')
   - Fetch team name from existing `GET /teams/my` query (same pattern as manager/[teamId]/page.tsx)
   - Header: page title "{teamName} Analytics", link back to manager dashboard
   - Time range toggle: 3 buttons (Week / Month / Quarter) using Button component with variant="outline" for inactive, variant="default" for active
   - Tab toggle: 2 buttons (Team Overview / Individual Member) styled as a tab bar
   - When activeTab='team': render `<TeamOverview teamId={teamId} range={range} />`
   - When activeTab='member': render placeholder `<div>Individual member view (coming soon)</div>` -- plan 08-03 will replace this
   - Add a link from the existing manager/[teamId]/page.tsx to the analytics page (small "Analytics" button/link in the header area next to Export CSV)
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Recharts installed. Analytics page accessible at /manager/{teamId}/analytics with tab toggle and time range selector. SummaryCard component created and reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Build Team Overview component with 4 summary cards and 4 charts</name>
  <files>apps/web/src/components/analytics/team-overview.tsx</files>
  <action>
Create `apps/web/src/components/analytics/team-overview.tsx`:

Props: `{ teamId: string; range: AnalyticsRange }`

1. Data fetching:
   - `useQuery` with key `['team-analytics', teamId, range]`, calls `api.get<TeamAnalyticsResponse>(`/teams/${teamId}/analytics/team?range=${range}`)`
   - Show loading skeleton while fetching

2. Summary cards row (4 cards in a responsive grid: 1 col mobile, 2 cols md, 4 cols lg):
   - Submission Rate: `{summary.submissionRate}%`, trend = submissionRateTrend, trendLabel="vs prev period"
   - Avg Hours/Day: `{summary.avgHoursPerDay}h`, trend = avgHoursPerDayTrend
   - Stress Distribution: Use SummaryCard with children slot -- render a small horizontal stacked bar showing low (green) / medium (yellow) / high (red) proportions
   - Task Count: `{summary.taskCount}`, trend = taskCountTrend

3. Charts (2-column grid on lg, 1-column on mobile):

   a. **Submission Rate Line Chart** (Recharts `LineChart`):
      - X-axis: dates (formatted as 'M/d'), Y-axis: 0-100%
      - Single blue line plotting `submissionRates[].rate`
      - Tooltip showing date, rate%, submitted/total counts
      - ResponsiveContainer for auto-sizing

   b. **Workload Heatmap** (custom implementation since Recharts has no native heatmap):
      - Render as a CSS grid/table: columns = dates, rows = members
      - Each cell colored by hours: green (`bg-green-200` for <=8h), yellow (`bg-yellow-200` for 8-10h), red (`bg-red-200` for >10h), gray (`bg-gray-100` for 0h)
      - Show hours number in each cell
      - Member names as row headers, dates as column headers (formatted short)
      - Horizontal scroll if too many days
      - Data source: group `heatmap[]` by userId for rows, by date for columns

   c. **Stress Trend Stacked Area Chart** (Recharts `AreaChart`):
      - X-axis: dates, Y-axis: count of members
      - 3 stacked areas: low (green-400), medium (yellow-400), high (red-400)
      - Use `stackId="stress"` on each Area
      - Tooltip showing date and count per level

   d. **Task Volume by Member Horizontal Bar Chart** (Recharts `BarChart` with `layout="vertical"`):
      - Y-axis: member names (displayName), X-axis: task count
      - Single bar per member, sorted by taskCount descending
      - Blue bars
      - Tooltip showing member name and count

All charts wrapped in Card components with CardHeader titles. Use `ResponsiveContainer width="100%" height={300}` for consistent sizing (heatmap can be taller based on member count).

Handle empty state: if no data for the period, show a centered message "No report data for this period" instead of empty charts.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Team Overview tab displays 4 summary KPI cards with trend arrows, submission rate line chart, workload heatmap with color coding, stress trend stacked area chart, and task volume horizontal bar chart. All charts respond to time range changes.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes with no errors
- Navigate to /manager/{teamId}/analytics in browser
- Team Overview tab is default, shows 4 summary cards and 4 charts
- Clicking Week/Month/Quarter re-fetches data and charts update
- Heatmap cells show green/yellow/red based on hours thresholds
- Charts are responsive on mobile (single column)
</verification>

<success_criteria>
Owner can navigate to /manager/{teamId}/analytics and see a Team Overview dashboard with 4 KPI summary cards (submission rate, avg hours, stress distribution, task count with trend arrows) and 4 charts (submission rate line, workload heatmap, stress trend stacked area, task volume horizontal bar). Time range toggle (week/month/quarter) updates all data.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-analytics/08-02-SUMMARY.md`
</output>
