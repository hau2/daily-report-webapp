---
phase: 06-membership-management
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/web/src/app/(dashboard)/teams/[id]/page.tsx
  - apps/web/src/app/(dashboard)/teams/page.tsx
autonomous: true
requirements:
  - MEMB-01
  - MEMB-02
  - MEMB-03
  - MEMB-04
  - MEMB-05

must_haves:
  truths:
    - "Owner sees a Remove button next to each non-owner member and can click it to remove them"
    - "Member sees a Leave Team button on the team detail page and can click it to leave"
    - "Owner sees a Transfer Ownership option next to each non-owner member"
    - "Owner sees pending invitations listed with a Cancel button for each"
    - "Owner sees a Delete Team button with a confirmation dialog"
    - "After removing a member or member leaving, the team disappears from that member's team list"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      provides: "Remove member, transfer ownership, cancel invitation, delete team, leave team UI"
    - path: "apps/web/src/app/(dashboard)/teams/page.tsx"
      provides: "Updated teams list (no changes expected, just verify departed teams disappear)"
  key_links:
    - from: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      to: "/teams/:id/members/:userId"
      via: "api.delete for remove member"
      pattern: "api\\.delete.*members"
    - from: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      to: "/teams/:id/leave"
      via: "api.post for leave team"
      pattern: "api\\.post.*leave"
    - from: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      to: "/teams/:id/transfer-ownership"
      via: "api.post for transfer ownership"
      pattern: "api\\.post.*transfer-ownership"
    - from: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      to: "/teams/:id/invitations/:email"
      via: "api.delete for cancel invitation"
      pattern: "api\\.delete.*invitations"
    - from: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      to: "/teams/:id"
      via: "api.delete for delete team"
      pattern: "api\\.delete.*teams"
---

<objective>
Add frontend UI for all membership management actions on the team detail page.

Purpose: Give owners controls to manage members (remove, transfer ownership), invitations (view pending, cancel), and the team itself (delete). Give members a way to leave a team.
Output: Updated team detail page with all membership management UI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-membership-management/06-01-SUMMARY.md
@apps/web/src/app/(dashboard)/teams/[id]/page.tsx
@apps/web/src/app/(dashboard)/teams/page.tsx
@apps/web/src/lib/api-client.ts

<interfaces>
<!-- API endpoints created in Plan 01 -->

New API endpoints (all require AccessTokenGuard):
- DELETE /teams/:id/members/:userId — owner-only (TeamManagerGuard), removes member
- POST /teams/:id/leave — any member, leaves team
- POST /teams/:id/transfer-ownership — owner-only, body: { targetUserId: string }
- GET /teams/:id/invitations — owner-only, returns pending invitations
- DELETE /teams/:id/invitations/:email — owner-only, cancels invitation
- DELETE /teams/:id — owner-only, deletes team

Expected response shapes:
- Remove/Leave/Transfer/Cancel: { message: string }
- GET invitations: Array<{ email: string, invitedAt: string, expiresAt: string }>
- Delete team: { message: string }

From apps/web/src/lib/api-client.ts:
```typescript
// api client has .get, .post, .patch, .delete methods
// all auto-include credentials (cookies)
```

Existing UI components used on team detail page:
- Card, CardContent, CardHeader, CardTitle, CardDescription (shadcn)
- Button (shadcn)
- Badge (shadcn)
- Form, FormField, etc. (react-hook-form + shadcn)
- toast from sonner
- useQuery, useMutation from @tanstack/react-query
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Team detail page -- membership management UI</name>
  <files>apps/web/src/app/(dashboard)/teams/[id]/page.tsx</files>
  <action>
    Extend the existing team detail page with all membership management actions. The page already shows members list and invite form (for owners).

    **For owners -- member row actions:**
    - Add a dropdown menu (or action buttons) to each non-owner member row:
      - "Remove" button: calls `api.delete(/teams/${id}/members/${m.userId})`, shows confirmation dialog (use window.confirm or a simple AlertDialog), invalidates ['teams', id, 'members'] query on success, shows toast
      - "Transfer Ownership" button: calls `api.post(/teams/${id}/transfer-ownership, { targetUserId: m.userId })`, shows confirmation dialog ("This will make {name} the owner and you will become a regular member"), invalidates both ['teams', 'my'] and ['teams', id, 'members'] queries on success, shows toast

    **For members (non-owners) -- leave team:**
    - Show a "Leave Team" button (use destructive variant) in the page header area or below the members list
    - On click: confirmation dialog ("Are you sure you want to leave {teamName}?")
    - Calls `api.post(/teams/${id}/leave)`
    - On success: redirect to /teams using router.push, show toast
    - Do NOT show this button if user is the owner

    **For owners -- pending invitations section:**
    - Add a new Card below members list and above the invite form
    - Query `GET /teams/${id}/invitations` to fetch pending invitations
    - Show each pending invitation with email and a "Cancel" button
    - Cancel button: calls `api.delete(/teams/${id}/invitations/${encodeURIComponent(email)})`, invalidates query, shows toast
    - If no pending invitations, show "No pending invitations" text

    **For owners -- delete team:**
    - Add a "Danger Zone" Card at the bottom of the page (red border, destructive styling)
    - "Delete Team" button with destructive variant
    - On click: confirmation dialog ("This will permanently delete {teamName} and all its data. Type the team name to confirm" -- use window.prompt or a simple confirm dialog)
    - Calls `api.delete(/teams/${id})`
    - On success: redirect to /teams using router.push, invalidate ['teams', 'my'], show toast

    **Implementation notes:**
    - Use useMutation for all actions
    - Use useQueryClient for cache invalidation
    - Import `useRouter` from next/navigation for redirects
    - Keep the existing InviteForm component intact
    - The api client may not have a `.delete` method -- check the api-client.ts. If not, add one that calls fetch with method: 'DELETE'. Pattern: same as .post but method: 'DELETE'.
    - For confirmation dialogs, use simple `window.confirm()` for now (keeps it simple, no new component needed)
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    Owner can see Remove and Transfer Ownership actions on member rows.
    Owner can see pending invitations with Cancel buttons.
    Owner can see Delete Team in danger zone.
    Member can see Leave Team button.
    All actions call correct API endpoints, show confirmations, handle success/error with toasts.
    TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify api-client supports DELETE and teams list page works after departure</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/app/(dashboard)/teams/page.tsx</files>
  <action>
    1. Check `apps/web/src/lib/api-client.ts` for a `.delete` method. If missing, add:
       ```typescript
       async delete<T>(path: string): Promise<T> {
         // Same pattern as existing .post/.patch but with method: 'DELETE' and no body
       }
       ```

    2. Verify the teams list page (`/teams`) works correctly after a member leaves or is removed:
       - The page queries `GET /teams/my` which already filters by active membership (left_at is null from Plan 01)
       - No changes should be needed, but verify the types are compatible
       - Run tsc to confirm

    3. If the api-client uses a generic request method internally, just ensure DELETE is supported through it.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>api-client has DELETE support, teams list page correctly shows only active teams, no type errors</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm exec tsc --noEmit` — no type errors
- `pnpm dev` starts without errors
- Team detail page renders all new UI elements for owner role
- Team detail page renders Leave Team button for member role
</verification>

<success_criteria>
- Owner sees member management actions (remove, transfer ownership) on team detail page
- Owner sees pending invitations with cancel capability
- Owner sees delete team option in danger zone
- Member sees leave team button
- All actions use correct API endpoints with proper error handling
- Confirmation dialogs prevent accidental destructive actions
- After leave/remove, user is redirected and team disappears from their list
</success_criteria>

<output>
After completion, create `.planning/phases/06-membership-management/06-02-SUMMARY.md`
</output>
