---
phase: 07-stress-level-tracking
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/src/app/(dashboard)/reports/[date]/page.tsx
  - apps/web/src/app/(dashboard)/manager/[teamId]/page.tsx
autonomous: true
requirements:
  - STRESS-01
  - STRESS-02

must_haves:
  truths:
    - "Member sees a stress level selector (Low / Medium / High) on the daily report page when report is draft and has tasks"
    - "Member can choose a stress level before clicking Submit Report"
    - "Owner can see each member's selected stress level when viewing that member's daily report on the manager dashboard"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/reports/[date]/page.tsx"
      provides: "StressLevelSelector component and integration with submit flow"
      contains: "stressLevel"
    - path: "apps/web/src/app/(dashboard)/manager/[teamId]/page.tsx"
      provides: "Stress level display in MemberReportCard"
      contains: "stressLevel"
  key_links:
    - from: "apps/web/src/app/(dashboard)/reports/[date]/page.tsx"
      to: "/api/reports/:id/submit"
      via: "api.post with stressLevel in body"
      pattern: "stressLevel"
    - from: "apps/web/src/app/(dashboard)/manager/[teamId]/page.tsx"
      to: "TeamMemberReport.stressLevel"
      via: "member.stressLevel display"
      pattern: "stressLevel"
---

<objective>
Add stress level UI to the daily report page (member selects before submitting) and display stress level on the manager dashboard (owner sees each member's stress).

Purpose: Completes STRESS-01 (member selection) and STRESS-02 (owner visibility) with frontend implementations.
Output: Updated report page with stress selector, updated manager dashboard showing stress levels.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-stress-level-tracking/07-01-SUMMARY.md

@apps/web/src/app/(dashboard)/reports/[date]/page.tsx
@apps/web/src/app/(dashboard)/manager/[teamId]/page.tsx

<interfaces>
<!-- From 07-01 plan (shared types updated) -->

From packages/shared/src/types/task.ts (after 07-01):
```typescript
export type StressLevel = 'low' | 'medium' | 'high';

export interface DailyReport {
  id: string;
  userId: string;
  teamId: string;
  reportDate: string;
  status: 'draft' | 'submitted';
  stressLevel: StressLevel | null;
  submittedAt: string | null;
  createdAt: string;
  updatedAt: string;
}
```

From packages/shared/src/types/manager.ts (after 07-01):
```typescript
export interface TeamMemberReport {
  userId: string;
  email: string;
  displayName: string;
  status: 'draft' | 'submitted' | 'none';
  stressLevel: StressLevel | null;
  report: DailyReport | null;
  tasks: Task[];
  totalHours: number;
  departed?: boolean;
}
```

Backend endpoint (after 07-01):
POST /reports/:id/submit  body: { stressLevel?: "low" | "medium" | "high" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stress level selector to daily report page</name>
  <files>apps/web/src/app/(dashboard)/reports/[date]/page.tsx</files>
  <action>
1. Import `StressLevel` from `@daily-report/shared`.

2. Create a `StressLevelSelector` component inside the file:
   - Props: `value: StressLevel | null`, `onChange: (level: StressLevel) => void`
   - Renders three buttons in a row: "Low", "Medium", "High"
   - Each button has a colored indicator:
     - Low: green (bg-green-100 text-green-700, selected: bg-green-600 text-white)
     - Medium: yellow (bg-yellow-100 text-yellow-700, selected: bg-yellow-500 text-white)
     - High: red (bg-red-100 text-red-700, selected: bg-red-600 text-white)
   - Selected button has solid background; unselected has light background
   - Label above: "How stressed are you feeling?" in text-sm font-medium

3. In the main `DailyReportPage` component:
   - Add state: `const [stressLevel, setStressLevel] = useState<StressLevel | null>(null);`
   - When `reportData` loads and report has a stressLevel, initialize the state with it (useEffect)
   - Show `StressLevelSelector` between the task list card and the Submit button, only when:
     - Report exists AND status is 'draft' AND tasks.length > 0
   - The selector is optional -- the user can submit without selecting (stressLevel stays null)

4. Update the `submitMutation`:
   - Change `mutationFn` from `api.post(\`/reports/${reportId}/submit\`)` to `api.post(\`/reports/${reportId}/submit\`, { stressLevel })` (pass current stressLevel state, which may be null -- backend handles null as no stress level, but only send stressLevel in body if it's not null)
   - The `handleSubmit` function should pass the report id; the mutation should include the stressLevel from component state

5. When report is submitted (status === 'submitted'), show the stress level as a read-only badge next to the "Submitted" status badge:
   - If `report.stressLevel` exists, show a badge with appropriate color (same color scheme as selector)
   - Format: "Stress: Low" / "Stress: Medium" / "Stress: High"
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp && cd apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - Stress level selector visible on draft report page when tasks exist
    - Three buttons (Low/Medium/High) with color coding
    - Selected stress level sent with submit request
    - Submitted reports show stress level as a colored badge
    - Stress level selection is optional (can submit without choosing)
  </done>
</task>

<task type="auto">
  <name>Task 2: Display stress level on manager dashboard</name>
  <files>apps/web/src/app/(dashboard)/manager/[teamId]/page.tsx</files>
  <action>
1. Import `StressLevel` from `@daily-report/shared`.

2. Create a `StressLevelBadge` component:
   - Props: `stressLevel: StressLevel | null`
   - If null, return null (don't show anything)
   - Returns a badge with color matching the level:
     - low: bg-green-100 text-green-700, label "Low"
     - medium: bg-yellow-100 text-yellow-700, label "Medium"
     - high: bg-red-100 text-red-700, label "High"

3. In `MemberReportCard`:
   - Add `StressLevelBadge` next to `StatusBadge` in the card header, using `member.stressLevel`
   - Only shows when member has submitted (status === 'submitted') and stressLevel is not null

4. The badge should appear inline with existing badges (after StatusBadge, before the Departed badge if present).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp && cd apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>
    - Manager dashboard shows stress level badge for each member who submitted with a stress level
    - Badge colors match the level (green/yellow/red)
    - No badge shown for members without stress level or without submitted report
  </done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` passes in `apps/web`
- Report page shows stress selector for draft reports with tasks
- Manager dashboard shows stress badges on submitted reports
</verification>

<success_criteria>
- Member can select Low/Medium/High stress on daily report before submitting
- Stress level persists through submit (sent to API)
- Owner sees stress level badge on manager dashboard for each member
- Both pages compile and render without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-stress-level-tracking/07-02-SUMMARY.md`
</output>
