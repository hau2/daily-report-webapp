---
phase: 03-task-management-and-daily-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/003_task_management.sql
  - packages/shared/src/types/task.ts
  - packages/shared/src/schemas/task.schema.ts
  - packages/shared/src/index.ts
  - apps/api/test/setup.ts
  - apps/api/src/tasks/tasks.service.spec.ts
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04, TASK-05, TASK-06]

must_haves:
  truths:
    - "daily_reports and tasks tables exist with correct schema"
    - "Shared TypeScript types and Zod schemas are importable from @daily-report/shared"
    - "Wave 0 test stubs exist and fail (RED state)"
  artifacts:
    - path: "database/migrations/003_task_management.sql"
      provides: "daily_reports and tasks DDL"
      contains: "CREATE TABLE IF NOT EXISTS daily_reports"
    - path: "packages/shared/src/types/task.ts"
      provides: "Task, DailyReport, DailyReportWithTasks interfaces"
      exports: ["Task", "DailyReport", "DailyReportWithTasks"]
    - path: "packages/shared/src/schemas/task.schema.ts"
      provides: "createTaskSchema, updateTaskSchema Zod schemas"
      exports: ["createTaskSchema", "updateTaskSchema", "CreateTaskInput", "UpdateTaskInput"]
    - path: "apps/api/src/tasks/tasks.service.spec.ts"
      provides: "Wave 0 RED test stubs for TASK-01 through TASK-06"
      min_lines: 30
  key_links:
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/types/task.ts"
      via: "re-export"
      pattern: "export.*Task.*from.*task"
---

<objective>
Create the database schema, shared types/schemas, and Wave 0 test stubs for task management and daily reports.

Purpose: Establish the data layer foundation that Plan 02 (backend) and Plan 03 (frontend) build against. Wave 0 stubs define the exact behaviors the service must implement.
Output: SQL migration file, shared TypeScript types and Zod schemas, failing test stubs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-task-management-and-daily-reports/03-RESEARCH.md

@database/migrations/002_team_management.sql
@packages/shared/src/index.ts
@packages/shared/src/types/team.ts
@packages/shared/src/schemas/team.schema.ts
@apps/api/test/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and shared types/schemas</name>
  <files>database/migrations/003_task_management.sql, packages/shared/src/types/task.ts, packages/shared/src/schemas/task.schema.ts, packages/shared/src/index.ts</files>
  <action>
1. Create `database/migrations/003_task_management.sql` following the pattern from 001 and 002 migrations:
   - `daily_reports` table: id (UUID PK default gen_random_uuid()), user_id (UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE), team_id (UUID NOT NULL REFERENCES teams(id) ON DELETE CASCADE), report_date (DATE NOT NULL), status (TEXT NOT NULL DEFAULT 'draft' CHECK status IN ('draft', 'submitted')), submitted_at (TIMESTAMPTZ), created_at (TIMESTAMPTZ NOT NULL DEFAULT now()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT now()). UNIQUE constraint on (user_id, team_id, report_date).
   - `tasks` table: id (UUID PK default gen_random_uuid()), report_id (UUID NOT NULL REFERENCES daily_reports(id) ON DELETE CASCADE), title (TEXT NOT NULL), estimated_hours (NUMERIC(4,2) NOT NULL CHECK estimated_hours > 0 AND estimated_hours <= 24), source_link (TEXT), notes (TEXT), sort_order (INTEGER NOT NULL DEFAULT 0), created_at (TIMESTAMPTZ NOT NULL DEFAULT now()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT now()).
   - Indexes: idx_daily_reports_user_date on (user_id, report_date), idx_daily_reports_team_date on (team_id, report_date), idx_tasks_report_id on (report_id).
   - Triggers: reuse existing update_updated_at_column() function for both tables (same pattern as 001/002).

2. Create `packages/shared/src/types/task.ts` with interfaces exactly as specified in RESEARCH.md:
   - `Task` interface: id, reportId, title, estimatedHours, sourceLink (string | null), notes (string | null), sortOrder, createdAt, updatedAt
   - `DailyReport` interface: id, userId, teamId, reportDate (string YYYY-MM-DD), status ('draft' | 'submitted'), submittedAt (string | null), createdAt, updatedAt
   - `DailyReportWithTasks` interface: report (DailyReport), tasks (Task[]), totalHours (number)

3. Create `packages/shared/src/schemas/task.schema.ts` with Zod schemas:
   - `createTaskSchema`: title (string min 1 max 500), estimatedHours (number positive max 24), sourceLink (string url max 2000 optional or literal ''), notes (string max 2000 optional), reportDate (string regex /^\d{4}-\d{2}-\d{2}$/), teamId (string uuid)
   - `updateTaskSchema`: title (string min 1 max 500 optional), estimatedHours (number positive max 24 optional), sourceLink (string url max 2000 optional or literal '' or null), notes (string max 2000 optional or null)
   - Export inferred types: CreateTaskInput, UpdateTaskInput

4. Update `packages/shared/src/index.ts` to re-export:
   - Types: Task, DailyReport, DailyReportWithTasks from './types/task'
   - Schemas: createTaskSchema, updateTaskSchema from './schemas/task.schema'
   - Schema types: CreateTaskInput, UpdateTaskInput from './schemas/task.schema'
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp && cd packages/shared && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Migration file exists with both tables, indexes, and triggers. Shared types and schemas compile and are re-exported from index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 -- extend mock query builder and write failing test stubs</name>
  <files>apps/api/test/setup.ts, apps/api/src/tasks/tasks.service.spec.ts</files>
  <action>
1. Extend `apps/api/test/setup.ts` MockQueryBuilder if needed. Check if `upsert` method is present -- if not, add it as `vi.fn().mockReturnThis()`. Also add `gt`, `lt`, `gte`, `lte` if not present (may be needed for date range queries). Keep existing methods intact.

2. Create `apps/api/src/tasks/tasks.service.spec.ts` with Wave 0 RED stubs following the pattern from `teams.service.spec.ts`:
   - Import TasksService (will not exist yet -- Vitest resolves as undefined, tests fail at assertion level)
   - Import createMockSupabaseService from test/setup
   - Use describe/it blocks with deliberately failing assertions: `expect(true).toBe(false)`
   - Test stubs to create (one `it` per behavior):

   describe('TasksService'):
     describe('createTask'):
       it('should auto-create daily_report if none exists and insert task') -- TASK-01
       it('should add task to existing draft report') -- TASK-01
       it('should reject task creation on submitted report') -- TASK-01/TASK-06

     describe('updateTask'):
       it('should update task fields on draft report') -- TASK-02/TASK-05
       it('should reject update on submitted report') -- TASK-02

     describe('deleteTask'):
       it('should delete task from draft report') -- TASK-03
       it('should reject delete on submitted report') -- TASK-03

     describe('getDailyReport'):
       it('should return report with tasks for a given date') -- TASK-04
       it('should return null when no report exists for date') -- TASK-04

     describe('submitReport'):
       it('should change status from draft to submitted with timestamp') -- TASK-06
       it('should reject submitting an already submitted report') -- TASK-06
       it('should reject submitting a report with zero tasks') -- TASK-06

   Each stub body: `expect(true).toBe(false); // RED: stub -- implement in Plan 02`
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/api && pnpm vitest run src/tasks/tasks.service.spec.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All 11 test stubs exist and fail (RED state). Existing tests in other spec files still pass. MockQueryBuilder extended with any new methods needed.</done>
</task>

</tasks>

<verification>
- `cd packages/shared && pnpm exec tsc --noEmit` passes (types compile)
- `cd apps/api && pnpm vitest run src/tasks/tasks.service.spec.ts` shows 11 failing tests (RED)
- `cd apps/api && pnpm vitest run src/auth` still passes (no regressions)
- `database/migrations/003_task_management.sql` exists with correct DDL
</verification>

<success_criteria>
- Migration file with daily_reports and tasks tables, indexes, triggers
- Shared Task, DailyReport, DailyReportWithTasks types exported
- Shared createTaskSchema, updateTaskSchema with inferred types exported
- 11 Wave 0 RED test stubs covering TASK-01 through TASK-06
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-management-and-daily-reports/03-01-SUMMARY.md`
</output>
