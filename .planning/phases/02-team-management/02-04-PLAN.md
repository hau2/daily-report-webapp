---
phase: 02-team-management
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03

must_haves:
  truths:
    - "User can create a team via the web UI and become its manager"
    - "Manager can send an invitation to a new email address via the team detail page"
    - "Invited user can click the link, be redirected to login if needed with token preserved, and accept the invitation"
    - "Accepted member appears in team context on the dashboard"
  artifacts:
    - path: "apps/api/src/teams/teams.service.ts"
      provides: "Backend service verified working end-to-end"
    - path: "apps/web/src/app/(auth)/join/page.tsx"
      provides: "Invitation accept page verified working"
  key_links:
    - from: "apps/web/src/app/(auth)/join/page.tsx"
      to: "POST /teams/invitations/accept"
      via: "full browser flow tested"
      pattern: "manual"
---

<objective>
Human verification of the complete Phase 2 end-to-end flows: team creation, invitation send, and invitation accept.

Purpose: Automated tests cover service-layer behavior. This checkpoint confirms the full browser-based user journey works correctly — including real email delivery (or dev log fallback), redirect flows, and UI state transitions.

Output: Phase 2 verified and ready for Phase 3.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-team-management/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 2 team management flows end-to-end</name>
  <action>
    Built in Plans 01-03:
    - Backend: POST /teams, GET /teams/my, POST /teams/:id/invitations, POST /teams/invitations/accept
    - Frontend: /teams list, /teams/new create form, /teams/[id] detail with invite form
    - Frontend: /join invitation accept page with auth redirect flow
    - Login page ?next= redirect support
    - Database migration: teams, team_members, team_invitations tables (002_team_management.sql)

    PREREQUISITES before testing:
    1. Apply the database migration in Supabase SQL editor:
       - Open Supabase dashboard → SQL Editor
       - Paste and run the contents of database/migrations/002_team_management.sql
       - Confirm: no errors; tables teams, team_members, team_invitations appear in Table Editor

    2. Start both servers: pnpm dev
       - Backend: http://localhost:3001
       - Frontend: http://localhost:3000

    FLOW 1 — Create a team (TEAM-01):
    3. Log in as an existing user at http://localhost:3000/login
    4. Navigate to http://localhost:3000/teams/new
    5. Enter a team name (e.g. "Engineering") and click "Create Team"
    6. Expect: redirect to /teams, team appears with "Manager" badge
    7. Click "View" — expect: /teams/[id] shows team name and invite form

    FLOW 2 — Send an invitation (TEAM-02):
    8. On /teams/[id] (as manager), enter a second email in the invite form
    9. Click "Send Invite"
    10. Expect: toast "Invitation sent!" and form clears
    11. Check server console: if RESEND_API_KEY is not set, look for:
        [DEV] Team invitation URL for <email>: http://localhost:3000/join?token=<jwt>
    12. Copy the /join?token=... URL from the logs

    FLOW 3 — Accept invitation (TEAM-03):
    13. Open an incognito window
    14. Visit the /join?token=... URL while logged out
    15. Expect: page shows "This invitation was sent to [email]" plus Log In and Register buttons
    16. Click "Log in" — expect: redirect to /login?next=/join?token=...
    17. Log in (register first if needed, using the same email the invitation was sent to)
    18. Expect: after login, redirect back to /join?token=...
    19. Click "Accept invitation"
    20. Expect: toast "You joined the team!" and redirect to /dashboard
    21. Navigate to /teams — expect: team appears with "Member" role badge

    FLOW 4 — Login ?next= redirect:
    22. Log out, then visit http://localhost:3000/teams
    23. Expect: redirect to /login
    24. Log in — expect: redirect back to /teams (or /dashboard)

    EDGE CASES (spot-check):
    25. Visit /join with no token — expect: "Invalid invitation link" message
    26. Try to accept an already-used token — expect: error toast from the API
  </action>
  <verify>All 26 steps above pass without unexpected errors</verify>
  <done>
    - TEAM-01: User created a team, visible in /teams with Manager badge
    - TEAM-02: Invitation sent, URL/email produced, form cleared with success toast
    - TEAM-03: Invited user followed link, logged in with token preserved, accepted, appears as Member in /teams
    - Type "approved" to continue, or describe which steps failed
  </done>
</task>

</tasks>

<verification>
Human verification of complete Phase 2 flows as described in Task 1 above. All 26 steps pass.
</verification>

<success_criteria>
- Manager can create a team and see themselves listed as Manager
- Manager can send an invitation (URL logged in dev mode or real email sent)
- Invited user can follow the link, log in with token preserved, accept, and see the team in /teams as Member
- All automated tests still passing: cd apps/api && pnpm vitest run — all green
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-management/02-04-SUMMARY.md` following the summary template.
</output>
