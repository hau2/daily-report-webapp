---
phase: 02-team-management
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/join/page.tsx
  - apps/web/src/app/(dashboard)/teams/page.tsx
  - apps/web/src/app/(dashboard)/teams/new/page.tsx
  - apps/web/src/app/(dashboard)/teams/[id]/page.tsx
  - apps/web/src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements:
  - TEAM-01
  - TEAM-02
  - TEAM-03

must_haves:
  truths:
    - "User can visit /teams/new, fill in a team name, and submit to create a team"
    - "Manager can visit /teams/[id], see current members, enter an email, and send an invitation"
    - "Logged-out user clicking an invitation link is redirected to /login?next=/join?token=... with the token preserved"
    - "Logged-in user visiting /join?token=... sees the target email address from the token payload and can click Accept"
    - "After accepting, user is redirected to /dashboard with a success toast"
    - "Dashboard page shows a prompt linking to /teams/new when user has no team"
    - "Login page redirects to ?next= param after successful login"
  artifacts:
    - path: "apps/web/src/app/(auth)/join/page.tsx"
      provides: "Invitation accept page — decodes token client-side to show invitee email, calls POST /teams/invitations/accept"
    - path: "apps/web/src/app/(dashboard)/teams/page.tsx"
      provides: "Teams list page — shows user's teams with role badge"
    - path: "apps/web/src/app/(dashboard)/teams/new/page.tsx"
      provides: "Create team form page"
    - path: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      provides: "Team detail page — member list + invite form (invite form shown only for managers)"
    - path: "apps/web/src/app/(auth)/login/page.tsx"
      provides: "Updated login page — reads ?next= and redirects there after successful login"
  key_links:
    - from: "apps/web/src/app/(auth)/join/page.tsx"
      to: "POST /teams/invitations/accept"
      via: "api.post('/teams/invitations/accept', { token })"
      pattern: "invitations/accept"
    - from: "apps/web/src/app/(auth)/login/page.tsx"
      to: "apps/web/src/app/(auth)/join/page.tsx"
      via: "router.push(next ?? '/dashboard') after login success"
      pattern: "searchParams.*next"
    - from: "apps/web/src/app/(dashboard)/teams/[id]/page.tsx"
      to: "POST /teams/:id/invitations"
      via: "api.post(`/teams/${id}/invitations`, { email })"
      pattern: "invitations"
---

<objective>
Build all frontend pages for team management: team creation, team detail with invite form, invitation accept page, and the login page ?next= redirect support.

Purpose: Completes the end-to-end user journeys for TEAM-01, TEAM-02, TEAM-03. The backend API (Plan 02) is fully built — this plan wires the UI onto it.

Output: Four new pages under (dashboard)/teams/ and (auth)/join/, plus updates to login and dashboard pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-team-management/02-RESEARCH.md
@.planning/phases/02-team-management/02-02-SUMMARY.md

<interfaces>
<!-- API endpoints available (from Plan 02): -->
<!-- POST   /teams                     body: { name }          → Team object -->
<!-- GET    /teams/my                  (auth)                  → Array<{ team: Team, role: 'manager'|'member' }> -->
<!-- POST   /teams/:id/invitations     body: { email }         → { message: 'Invitation sent' } (403 if not manager) -->
<!-- POST   /teams/invitations/accept  body: { token }         → { message, teamId } -->

<!-- Frontend conventions (from Phase 1): -->
<!-- import { api } from '@/lib/api-client'  — api.get<T>, api.post<T>, api.patch<T> -->
<!-- import { useAuth } from '@/hooks/use-auth'  — returns { user, isLoading, isAuthenticated, login, ... } -->
<!-- useMutation from @tanstack/react-query; useQuery for data fetching -->
<!-- toast from 'sonner'; useRouter from 'next/navigation'; useSearchParams from 'next/navigation' -->
<!-- UI components: Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter -->
<!-- Form, FormControl, FormField, FormItem, FormLabel, FormMessage from '@/components/ui/form' -->
<!-- Input, Button, Badge from '@/components/ui/badge', '@/components/ui/input', '@/components/ui/button' -->
<!-- react-hook-form + zodResolver(@hookform/resolvers/zod) -->
<!-- All pages under (dashboard) are server components by default; use 'use client' when needed -->

<!-- Shared types now available: -->
<!-- import type { Team, TeamMember, CreateTeamInput, InviteMemberInput, AcceptInvitationInput } from '@daily-report/shared' -->
<!-- import { createTeamSchema, inviteMemberSchema, acceptInvitationSchema } from '@daily-report/shared' -->

<!-- Token decoding for /join page: -->
<!-- Decode JWT payload client-side WITHOUT verification to show inviteeEmail to user -->
<!-- const [, payloadB64] = token.split('.'); -->
<!-- const payload = JSON.parse(atob(payloadB64)); -->
<!-- payload.inviteeEmail — the email the invitation was sent to -->
<!-- Actual verification happens on the backend -->

<!-- (auth) layout: does NOT redirect unauthenticated users (join page must be here) -->
<!-- (dashboard) layout: DOES redirect unauthenticated users (teams pages are here, correct) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page ?next= redirect and /join invitation accept page</name>
  <files>
    apps/web/src/app/(auth)/login/page.tsx,
    apps/web/src/app/(auth)/join/page.tsx
  </files>
  <action>
    1. Update `apps/web/src/app/(auth)/login/page.tsx`:
       - Add `'use client'` if not already present (it is)
       - Add `import { useSearchParams } from 'next/navigation'`
       - In the component, add: `const searchParams = useSearchParams(); const next = searchParams.get('next');`
       - In the loginMutation's `onSuccess`, change `router.push('/dashboard')` to `router.push(next ?? '/dashboard')`
       - Keep all existing form fields, styling, and links exactly as they are — this is a minimal targeted change

    2. Create `apps/web/src/app/(auth)/join/page.tsx` as a client component:
       ```
       'use client'
       - Read token from useSearchParams() — const token = searchParams.get('token')
       - If no token: show "Invalid invitation link" card with link to /login
       - Decode token payload client-side (no verification — backend verifies):
           const [, b64] = token.split('.')
           const payload = JSON.parse(atob(b64))
           const inviteeEmail: string = payload.inviteeEmail ?? ''
           const teamId: string = payload.teamId ?? ''
       - Use useAuth() to get { isAuthenticated, isLoading, user }
       - If isLoading: show loading spinner or "Loading..." text
       - If NOT isAuthenticated:
           Show card: "You've been invited to join a team"
           Show: "This invitation was sent to {inviteeEmail}. Please log in or register with that email address."
           Two buttons: "Log in" → router.push(`/login?next=/join?token=${token}`)
                        "Register" → router.push(`/register?next=/join?token=${token}`)
       - If authenticated:
           Show card with invitation details: team invite for {inviteeEmail}
           Show warning if user.email !== inviteeEmail: "Warning: You are logged in as {user.email} but this invitation is for {inviteeEmail}. The accept request will fail."
           "Accept invitation" button with loading state
           useMutation calling api.post<{ message: string; teamId: string }>('/teams/invitations/accept', { token })
           onSuccess: toast.success('You joined the team!'); router.push('/dashboard')
           onError: toast.error(error.message || 'Failed to accept invitation')
       ```
       - The page lives under (auth) route group — the layout there does NOT require authentication, which is correct for this landing page
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Login page reads ?next= param and redirects there after login instead of hardcoded /dashboard
    - /join page exists, handles: no token, unauthenticated user (redirects to login/register with token preserved), authenticated user (shows accept button)
    - TypeScript compiles cleanly in apps/web
  </done>
</task>

<task type="auto">
  <name>Task 2: Teams pages (/teams, /teams/new, /teams/[id]) and dashboard "no team" prompt</name>
  <files>
    apps/web/src/app/(dashboard)/teams/page.tsx,
    apps/web/src/app/(dashboard)/teams/new/page.tsx,
    apps/web/src/app/(dashboard)/teams/[id]/page.tsx,
    apps/web/src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
    1. Create `apps/web/src/app/(dashboard)/teams/page.tsx` (client component):
       - useQuery key ['teams', 'my'] fetching `api.get<Array<{ team: Team; role: 'manager' | 'member' }>>('/teams/my')`
       - Loading state: show skeleton or "Loading your teams..."
       - Empty state: Card with "You have no teams yet." and Button linking to /teams/new "Create a team"
       - Non-empty: list of Cards, each showing team name, role Badge ('Manager' vs 'Member'), and a "View" link to /teams/[id]
       - Page header: "My Teams" h1 with a "Create Team" Button (variant="default") linking to /teams/new
       - Use Link from next/link for navigation

    2. Create `apps/web/src/app/(dashboard)/teams/new/page.tsx` (client component):
       - react-hook-form with zodResolver(createTeamSchema)
       - Single field: name (text input, placeholder "e.g. Engineering")
       - useMutation: `api.post<Team>('/teams', data)`
       - onSuccess: toast.success('Team created!'); queryClient.invalidateQueries({ queryKey: ['teams', 'my'] }); router.push('/teams')
       - onError: toast.error(error.message || 'Failed to create team')
       - Standard Card layout matching settings page style (CardHeader with title "Create a Team", CardContent with form, CardFooter with submit Button)
       - Back link to /teams above the card

    3. Create `apps/web/src/app/(dashboard)/teams/[id]/page.tsx` (client component):
       - Accept `params: { id: string }` prop
       - useQuery key ['teams', id, 'detail'] fetching `api.get<{ team: Team; members: TeamMember[]; role: 'manager' | 'member' }>(`/teams/${id}`)`
         NOTE: This endpoint does not exist yet in Plan 02. Use GET /teams/my and filter by id for now, OR add a GET /teams/:id endpoint note. Since Plan 02 only built GET /teams/my, fetch that and find the relevant team. For members list, we need a separate approach. Given Plan 02 did not build GET /teams/:id/members, implement the page to: show the team name from the /teams/my data, and for members show a placeholder "Member list coming soon" until the endpoint exists. Show the invite form for managers.

         REVISED APPROACH to avoid scope creep: The team detail page should:
         - Fetch `/teams/my` to get the user's team list and determine their role in team `id`
         - If team not found in list: show "Team not found or you are not a member"
         - Show team name and user's role
         - For ALL users: show the team name
         - For MANAGERS only (role === 'manager'): show the invite form
         - Member list: show "Member management will be available once members join" (we have no GET endpoint for members yet — that would require a new backend endpoint not in scope for Phase 2)

       - Invite form (shown only when role === 'manager'):
           react-hook-form with zodResolver(inviteMemberSchema)
           Email input, "Send Invite" button
           useMutation: `api.post<{ message: string }>(`/teams/${id}/invitations`, { email })`
           onSuccess: toast.success('Invitation sent!'); form.reset()
           onError: toast.error(error.message || 'Failed to send invitation')

    4. Update `apps/web/src/app/(dashboard)/dashboard/page.tsx`:
       - Add a useQuery for ['teams', 'my'] to check if user has any teams
       - If teams list is empty (and not loading): show a Card or callout: "Get started — Create your first team to start logging daily reports." with a Button/Link to /teams/new
       - If teams list is non-empty: show the first team name with a link to /teams/[id]
       - Keep any existing dashboard content; this is an additive change
       - If dashboard/page.tsx does not exist yet (check the file path: (dashboard)/dashboard/page.tsx vs just (dashboard)/page.tsx): check the actual path and use the correct one. The Phase 1 plan built the dashboard at `apps/web/src/app/(dashboard)/dashboard/page.tsx` OR `apps/web/src/app/(dashboard)/page.tsx` — inspect which exists and edit that file.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - /teams page renders user's teams with create button
    - /teams/new page renders create form that POSTs to backend
    - /teams/[id] page renders team detail with invite form for managers
    - Dashboard page shows teams context or "create team" prompt
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit` — no TypeScript errors
2. Grep join page: `grep "invitations/accept\|searchParams\|isAuthenticated" apps/web/src/app/\(auth\)/join/page.tsx` — all three present
3. Grep login: `grep "next\|searchParams" apps/web/src/app/\(auth\)/login/page.tsx` — ?next= handling present
4. Confirm files exist: `ls apps/web/src/app/\(dashboard\)/teams/` — page.tsx, new/page.tsx, [id]/page.tsx
</verification>

<success_criteria>
- Login page redirects to ?next= param after successful login
- /join page handles unauthenticated (redirect to login/register with token) and authenticated (accept button) states
- /teams list page, /teams/new form page, /teams/[id] detail with invite form all exist
- Dashboard page has team context prompt
- TypeScript clean in apps/web
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-management/02-03-SUMMARY.md` following the summary template.
</output>
