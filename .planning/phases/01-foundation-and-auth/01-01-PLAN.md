---
phase: 01-foundation-and-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.json
  - .gitignore
  - .env.example
  - apps/api/package.json
  - apps/api/nest-cli.json
  - apps/api/tsconfig.json
  - apps/api/tsconfig.build.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/app.controller.ts
  - apps/api/src/app.service.ts
  - apps/api/prisma/schema.prisma
  - apps/api/src/prisma/prisma.module.ts
  - apps/api/src/prisma/prisma.service.ts
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/tsconfig.json
  - apps/web/postcss.config.mjs
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/lib/utils.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/types/user.ts
  - packages/shared/src/types/auth.ts
  - packages/shared/src/schemas/auth.schema.ts
  - packages/tsconfig/base.json
  - packages/tsconfig/nestjs.json
  - packages/tsconfig/nextjs.json
  - vitest.workspace.ts
  - apps/api/vitest.config.ts
  - apps/api/test/setup.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-04

must_haves:
  truths:
    - "pnpm dev starts both NestJS and Next.js concurrently via Turborepo"
    - "Prisma schema contains User model with all auth fields and generates successfully"
    - "PrismaService connects to Supabase PostgreSQL and is injectable in NestJS"
    - "Shared package exports User type and Zod auth schemas importable from both apps"
    - "Vitest runs and passes in the API workspace"
  artifacts:
    - path: "apps/api/src/main.ts"
      provides: "NestJS bootstrap with cookie parser, validation pipe, CORS"
      min_lines: 15
    - path: "apps/api/prisma/schema.prisma"
      provides: "Full database schema with User, Team, Task, ReportSubmission, Invitation models"
      contains: "model User"
    - path: "apps/api/src/prisma/prisma.service.ts"
      provides: "Injectable Prisma client with lifecycle hooks"
      exports: ["PrismaService"]
    - path: "packages/shared/src/index.ts"
      provides: "Barrel exports for shared types and schemas"
    - path: "vitest.workspace.ts"
      provides: "Monorepo Vitest workspace configuration"
    - path: "turbo.json"
      provides: "Turborepo pipeline configuration"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/prisma/prisma.module.ts"
      via: "NestJS module import"
      pattern: "PrismaModule"
    - from: "apps/api/prisma/schema.prisma"
      to: "apps/api/src/generated/prisma"
      via: "prisma generate output"
      pattern: 'output.*generated/prisma'
    - from: "turbo.json"
      to: "apps/api, apps/web"
      via: "Turborepo task pipeline"
      pattern: "dependsOn.*\\^build"
---

<objective>
Restructure the project from separate backend/frontend directories into a pnpm + Turborepo monorepo, set up the full Prisma schema with Supabase PostgreSQL, create the shared types package, and establish the Vitest test infrastructure.

Purpose: Every subsequent plan depends on the monorepo structure, database schema, shared types, and test framework being in place. This is the foundation that all auth, API, and frontend work builds on.

Output: Working monorepo at apps/api + apps/web + packages/shared, Prisma schema with all models, Vitest configured and running.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-auth/01-RESEARCH.md
@.planning/phases/01-foundation-and-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure into pnpm + Turborepo monorepo</name>
  <files>
    package.json, pnpm-workspace.yaml, turbo.json, .gitignore, .env.example,
    apps/api/package.json, apps/api/nest-cli.json, apps/api/tsconfig.json, apps/api/tsconfig.build.json,
    apps/api/src/main.ts, apps/api/src/app.module.ts, apps/api/src/app.controller.ts, apps/api/src/app.service.ts,
    apps/api/.eslintrc.js, apps/api/.prettierrc,
    apps/web/package.json, apps/web/next.config.ts, apps/web/tsconfig.json, apps/web/postcss.config.mjs,
    apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/web/src/app/globals.css,
    apps/web/src/lib/utils.ts,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts,
    packages/shared/src/types/user.ts, packages/shared/src/types/auth.ts,
    packages/shared/src/schemas/auth.schema.ts,
    packages/tsconfig/base.json, packages/tsconfig/nestjs.json, packages/tsconfig/nextjs.json
  </files>
  <action>
    **IMPORTANT:** The project currently has `backend/` and `frontend/` directories with vanilla NestJS 11 and Next.js 16 scaffolds using separate package managers (yarn and npm). These must be restructured into a pnpm monorepo.

    **Step 1: Remove old directories.** Delete `backend/` and `frontend/` directories entirely (they only contain boilerplate scaffolds with no custom code).

    **Step 2: Create root monorepo config.**
    - Create root `package.json` with `"private": true`, name `"daily-report"`, and scripts: `"dev": "turbo dev"`, `"build": "turbo build"`, `"lint": "turbo lint"`, `"test": "turbo test"`, `"db:generate": "turbo db:generate"`, `"db:push": "turbo db:push"`.
    - Create `pnpm-workspace.yaml` with packages: `["apps/*", "packages/*"]`.
    - Create `turbo.json` per the research document (build, dev, lint, test, db:generate, db:push tasks).
    - Create `.env.example` with all environment variables documented in the research (DATABASE_URL, DIRECT_URL, JWT secrets, PORT, FRONTEND_URL, RESEND_API_KEY, EMAIL_FROM, NESTJS_API_URL).
    - Update `.gitignore` to cover: `node_modules/`, `.next/`, `dist/`, `.env`, `.env.local`, `.DS_Store`, `coverage/`, `*.tsbuildinfo`, `.turbo/`, `apps/api/src/generated/`.

    **Step 3: Create `packages/tsconfig/`** with shared TypeScript configs:
    - `base.json`: strict mode, ES2022 target, moduleResolution bundler, esModuleInterop, skipLibCheck, declaration, declarationMap, sourceMap.
    - `nestjs.json`: extends base, module commonjs, emitDecoratorMetadata, experimentalDecorators.
    - `nextjs.json`: extends base, module esnext, jsx preserve, plugins for next.

    **Step 4: Create `packages/shared/`** with:
    - `package.json`: name `"@daily-report/shared"`, main `"src/index.ts"`, deps: `zod ^4.3`.
    - `tsconfig.json`: extends `@daily-report/tsconfig/base.json`.
    - `src/types/user.ts`: Export `User` interface with fields: `id: string`, `email: string`, `displayName: string | null`, `timezone: string`, `emailVerified: boolean`, `createdAt: Date`, `updatedAt: Date`.
    - `src/types/auth.ts`: Export `LoginRequest`, `RegisterRequest`, `ForgotPasswordRequest`, `ResetPasswordRequest`, `AuthResponse` interfaces.
    - `src/schemas/auth.schema.ts`: Export Zod schemas: `loginSchema` (email + password min 8), `registerSchema` (email + password min 8 + displayName optional), `forgotPasswordSchema` (email), `resetPasswordSchema` (token + password min 8). Export inferred types from schemas.
    - `src/index.ts`: Barrel export all types and schemas.

    **Step 5: Create `apps/api/`** (NestJS backend):
    - `package.json`: name `"@daily-report/api"`, NestJS ^11 deps (already v11 in the old scaffold), add `@nestjs/config`, `cookie-parser`, `class-validator`, `class-transformer`. Scripts: `"dev": "nest start --watch"`, `"build": "nest build"`, `"start": "node dist/main"`, `"test": "vitest run"`, `"db:generate": "prisma generate"`, `"db:push": "prisma db push"`. Add `@daily-report/shared` as workspace dependency: `"@daily-report/shared": "workspace:*"`.
    - `tsconfig.json`: extends `@daily-report/tsconfig/nestjs.json`, paths for `@/*` to `src/*`.
    - `tsconfig.build.json`: extends tsconfig.json, excludes node_modules, test, dist, spec files.
    - `nest-cli.json`: sourceRoot `src`, compilerOptions with `deleteOutDir: true`.
    - `src/main.ts`: NestJS bootstrap with cookie-parser, global ValidationPipe (whitelist: true, forbidNonWhitelisted: true, transform: true), CORS enabled for `process.env.FRONTEND_URL` with credentials: true, listen on `process.env.PORT ?? 3001`. Import cookie-parser with `import * as cookieParser from 'cookie-parser'`.
    - `src/app.module.ts`: Import ConfigModule.forRoot({ isGlobal: true }) and PrismaModule. Keep AppController and AppService for health check.
    - `src/app.controller.ts`: Simple health check GET `/` returning `{ status: 'ok' }`.
    - `src/app.service.ts`: Health check service.
    - Add `@types/cookie-parser` to devDependencies.

    **Step 6: Create `apps/web/`** (Next.js frontend):
    - `package.json`: name `"@daily-report/web"`, deps: `next 16.1.6`, `react 19.2.3`, `react-dom 19.2.3`, `react-hook-form ^7.54`, `@hookform/resolvers ^3.9`, `zod ^4.3`. Add `@daily-report/shared` as workspace dependency. Scripts: `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`, `"lint": "eslint"`.
    - `tsconfig.json`: extends `@daily-report/tsconfig/nextjs.json`, paths `@/*` to `src/*`.
    - `next.config.ts`: Default Next.js config (empty for now).
    - `postcss.config.mjs`: Tailwind CSS v4 PostCSS plugin.
    - `src/app/globals.css`: Tailwind v4 CSS-first config with `@import "tailwindcss"`.
    - `src/app/layout.tsx`: Root layout with Inter font, html lang, body with globals.css.
    - `src/app/page.tsx`: Simple "Daily Report" landing page (placeholder).
    - `src/lib/utils.ts`: Export `cn()` utility for className merging (clsx + twMerge pattern from shadcn/ui). Add `clsx` and `tailwind-merge` to deps.

    **Step 7: Initialize shadcn/ui** in `apps/web/`:
    - Run `npx shadcn@latest init` (or manually create `components.json`). Use "new-york" style, zinc base color, CSS variables enabled.
    - Pre-install: Button, Input, Label, Card, Toast (Sonner), Form components.

    **Step 8: Install all dependencies.** Run `pnpm install` from root. Verify `pnpm dev` starts both apps.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp && pnpm install && pnpm build</automated>
  </verify>
  <done>
    - Root `pnpm install` completes without errors
    - `pnpm build` succeeds for all workspaces
    - `apps/api/` contains NestJS with ConfigModule and PrismaModule imported
    - `apps/web/` contains Next.js with Tailwind v4 and shadcn/ui initialized
    - `packages/shared/` exports User type and Zod auth schemas
    - `packages/tsconfig/` provides shared TypeScript configs
    - Old `backend/` and `frontend/` directories are removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up Prisma schema and database connection</name>
  <files>
    apps/api/prisma/schema.prisma,
    apps/api/src/prisma/prisma.module.ts,
    apps/api/src/prisma/prisma.service.ts,
    apps/api/src/app.module.ts
  </files>
  <action>
    **Step 1: Create Prisma schema** at `apps/api/prisma/schema.prisma` with the EXACT schema from the research document:
    - Generator: `prisma-client` provider, output `"../src/generated/prisma"`, `moduleFormat = "cjs"` (CRITICAL for NestJS CommonJS compatibility).
    - Datasource: `postgresql`, url from `DATABASE_URL` env, directUrl from `DIRECT_URL` env.
    - Models: `User`, `Team`, `TeamMember`, `Task`, `ReportSubmission`, `Invitation` -- copy exactly from the research `Pattern 3` section. All models use `@map()` for snake_case table/column names. User model must include: `id`, `email` (unique), `passwordHash`, `displayName`, `timezone` (default "UTC"), `emailVerified` (default false), `refreshTokenHash`, `createdAt`, `updatedAt`, plus relations.

    **Step 2: Add Prisma dependencies** to `apps/api/package.json`:
    - `prisma` (devDep), `@prisma/client`, `@prisma/adapter-pg`, `pg` (deps), `@types/pg` (devDep).

    **Step 3: Run `prisma generate`** to create the generated client at `apps/api/src/generated/prisma/`.

    **Step 4: Create PrismaService** at `apps/api/src/prisma/prisma.service.ts`:
    - Extend PrismaClient, implement OnModuleInit and OnModuleDestroy.
    - Import from `'../generated/prisma/client'`.
    - Call `$connect()` in onModuleInit, `$disconnect()` in onModuleDestroy.

    **Step 5: Create PrismaModule** at `apps/api/src/prisma/prisma.module.ts`:
    - Global module (`@Global()`) that provides and exports PrismaService.

    **Step 6: Update AppModule** to import PrismaModule (if not already done in Task 1).

    **Step 7: Run `prisma db push`** to sync schema to the database (requires DATABASE_URL in .env -- if not available, verify `prisma generate` passes and document that db push needs a real connection).

    **NOTE:** If no .env with DATABASE_URL exists yet, `prisma generate` should still succeed (it only needs the schema file). `prisma db push` can be deferred to when the user configures their Supabase connection. The task passes if generate works.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp && pnpm --filter @daily-report/api exec prisma generate && pnpm build</automated>
  </verify>
  <done>
    - Prisma schema has all 6 models (User, Team, TeamMember, Task, ReportSubmission, Invitation)
    - `prisma generate` produces client at `apps/api/src/generated/prisma/`
    - PrismaService is injectable and imported in AppModule via PrismaModule
    - `moduleFormat = "cjs"` is set in generator block
    - `pnpm build` still passes with Prisma client generated
  </done>
</task>

<task type="auto">
  <name>Task 3: Set up Vitest test infrastructure (Wave 0)</name>
  <files>
    vitest.workspace.ts,
    apps/api/vitest.config.ts,
    apps/api/test/setup.ts,
    apps/api/src/auth/auth.service.spec.ts,
    apps/api/src/auth/auth.controller.spec.ts,
    apps/api/src/users/users.service.spec.ts
  </files>
  <action>
    This task fulfills the Wave 0 requirements from the Validation Strategy.

    **Step 1: Install test dependencies.**
    - Root: `pnpm add -Dw vitest @vitest/coverage-v8`
    - API: `pnpm --filter @daily-report/api add -D vitest unplugin-swc @swc/core @vitest/coverage-v8`

    **Step 2: Create `vitest.workspace.ts`** at monorepo root:
    ```typescript
    import { defineWorkspace } from 'vitest/config';
    export default defineWorkspace(['apps/api']);
    ```

    **Step 3: Create `apps/api/vitest.config.ts`:**
    ```typescript
    import { defineConfig } from 'vitest/config';
    import swc from 'unplugin-swc';
    export default defineConfig({
      test: {
        globals: true,
        root: './',
        setupFiles: ['./test/setup.ts'],
      },
      plugins: [swc.vite({ module: { type: 'commonjs' } })],
    });
    ```

    **Step 4: Create `apps/api/test/setup.ts`:**
    - Import and configure any global test utilities.
    - Create a mock PrismaService factory that returns jest-style mocks for all model methods (findUnique, findMany, create, update, delete).
    - Export `createMockPrismaService()` function for use in test files.
    - Import `vi` from vitest for mocking.

    **Step 5: Create test stub files** with descriptive pending tests (these will be filled in by Plans 02, 04, and 05):

    `apps/api/src/auth/auth.service.spec.ts`:
    - `describe('AuthService')` with `it.todo()` stubs for: "register - creates user with hashed password", "register - rejects duplicate email with 409", "verification - sends email with valid token on signup", "verify - marks user as verified with valid token", "forgot - sends reset email with valid token", "reset - validates token and updates password".

    `apps/api/src/auth/auth.controller.spec.ts`:
    - `describe('AuthController')` with `it.todo()` stubs for: "cookie - login sets httpOnly access + refresh cookies", "refresh - issues new access token with valid refresh token", "expired - expired access token triggers refresh".

    `apps/api/src/users/users.service.spec.ts`:
    - `describe('UsersService')` with `it.todo()` stubs for: "update name - changes display name", "update email - requires current password", "update password - requires current password and hashes new password".

    **Step 6: Update `apps/api/package.json`** test script to `"test": "vitest run --reporter=verbose"`.

    **Step 7: Run tests** to verify the infrastructure works (all tests should be in todo state, not failing).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp && pnpm --filter @daily-report/api exec vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - Vitest runs in the API workspace without errors
    - All test stubs show as "todo" (not failing, not passing -- pending state)
    - `apps/api/vitest.config.ts` uses SWC for NestJS decorator support
    - `apps/api/test/setup.ts` exports `createMockPrismaService()` helper
    - Test files exist for auth.service, auth.controller, and users.service
    - `pnpm test` from root triggers Vitest via Turborepo
  </done>
</task>

</tasks>

<verification>
1. `pnpm install` from root completes without errors
2. `pnpm build` builds all workspaces successfully
3. `pnpm --filter @daily-report/api exec prisma generate` creates client
4. `pnpm --filter @daily-report/api exec vitest run` runs without errors
5. `packages/shared/src/index.ts` exports User type and auth schemas
6. No `backend/` or `frontend/` directories remain
</verification>

<success_criteria>
- Monorepo structure is established with pnpm workspaces and Turborepo
- Prisma schema has all 6 models and generates successfully
- PrismaService is injectable in NestJS via global PrismaModule
- Shared package provides types and Zod schemas
- Vitest infrastructure is ready with test stubs for all Phase 1 requirements
- Both apps build and can be started with `pnpm dev`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-auth/01-01-SUMMARY.md`
</output>
