---
phase: 10-export-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/lib/export-png.ts
  - apps/web/src/lib/export-pdf.ts
  - apps/web/src/lib/export-csv.ts
  - apps/web/src/lib/__tests__/export-csv.test.ts
  - apps/web/src/components/analytics/chart-card.tsx
  - apps/web/src/components/analytics/export-toolbar.tsx
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03

must_haves:
  truths:
    - "Each chart card displays a download icon in its header that the owner can click"
    - "Owner can trigger a PDF download that captures all visible chart elements"
    - "CSV utility produces valid multi-section CSV from team analytics data"
    - "CSV utility produces valid multi-section CSV from member analytics data"
    - "ExportToolbar shows PDF and CSV download buttons"
  artifacts:
    - path: "apps/web/src/lib/export-png.ts"
      provides: "PNG export utility using html-to-image toPng"
      exports: ["exportPng"]
    - path: "apps/web/src/lib/export-pdf.ts"
      provides: "PDF generation utility using jspdf + html-to-image"
      exports: ["exportAnalyticsPdf"]
    - path: "apps/web/src/lib/export-csv.ts"
      provides: "CSV builder for team and member analytics data"
      exports: ["downloadCsv", "teamAnalyticsToCsv", "memberAnalyticsToCsv"]
    - path: "apps/web/src/lib/__tests__/export-csv.test.ts"
      provides: "Unit tests for CSV builder functions"
    - path: "apps/web/src/components/analytics/chart-card.tsx"
      provides: "Reusable chart wrapper with PNG download button"
      exports: ["ChartCard"]
    - path: "apps/web/src/components/analytics/export-toolbar.tsx"
      provides: "PDF and CSV export buttons bar"
      exports: ["ExportToolbar"]
  key_links:
    - from: "apps/web/src/components/analytics/chart-card.tsx"
      to: "apps/web/src/lib/export-png.ts"
      via: "import { exportPng }"
      pattern: "exportPng\\(cardRef"
    - from: "apps/web/src/components/analytics/export-toolbar.tsx"
      to: "apps/web/src/lib/export-pdf.ts"
      via: "import { exportAnalyticsPdf }"
      pattern: "exportAnalyticsPdf"
    - from: "apps/web/src/components/analytics/export-toolbar.tsx"
      to: "apps/web/src/lib/export-csv.ts"
      via: "import { downloadCsv, teamAnalyticsToCsv, memberAnalyticsToCsv }"
      pattern: "downloadCsv"
---

<objective>
Create export utility functions (PNG, PDF, CSV) and reusable UI components (ChartCard, ExportToolbar) for analytics export.

Purpose: Provides the building blocks that Plan 02 will wire into the existing analytics dashboard.
Output: 3 lib utility files, 1 test file, 2 new components, html-to-image and jspdf installed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-export-analytics/10-RESEARCH.md
@packages/shared/src/types/analytics.ts

<interfaces>
<!-- Analytics types the CSV builder must consume -->
From packages/shared/src/types/analytics.ts:
```typescript
export interface TeamAnalyticsResponse {
  range: AnalyticsRange;
  startDate: string;
  endDate: string;
  summary: TeamAnalyticsSummary;
  submissionRates: DailySubmissionRate[];
  heatmap: HeatmapCell[];
  stressTrend: DailyStressDistribution[];
  taskVolumeByMember: MemberTaskVolume[];
}

export interface MemberAnalyticsResponse {
  range: AnalyticsRange;
  startDate: string;
  endDate: string;
  userId: string;
  displayName: string;
  summary: MemberAnalyticsSummary;
  dailyHours: DailyHoursEntry[];
  stressTimeline: DailyStressEntry[];
  dailyTasks: DailyTaskCount[];
  submissionCalendar: SubmissionCalendarDay[];
}
```

From apps/web/src/components/ui/button.tsx:
```typescript
// Available size variants: "default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm"
// Available variants: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
```

From apps/web/src/components/ui/card.tsx:
```typescript
export { Card, CardContent, CardHeader, CardTitle }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create export utility functions</name>
  <files>apps/web/package.json, apps/web/src/lib/export-png.ts, apps/web/src/lib/export-pdf.ts, apps/web/src/lib/export-csv.ts, apps/web/src/lib/__tests__/export-csv.test.ts</files>
  <action>
1. Install html-to-image and jspdf in apps/web:
   `cd apps/web && pnpm add html-to-image jspdf`

2. Create `apps/web/src/lib/export-png.ts`:
   - Export async function `exportPng(element: HTMLElement, filename: string): Promise<void>`
   - Uses `toPng` from html-to-image with options: `{ cacheBust: true, backgroundColor: '#ffffff', pixelRatio: 2 }`
   - Creates an anchor element, sets download to `${filename}.png`, sets href to the data URL, clicks it

3. Create `apps/web/src/lib/export-pdf.ts`:
   - Export async function `exportAnalyticsPdf(chartElements: HTMLElement[], teamName: string, range: string): Promise<void>`
   - Uses `jsPDF` in landscape A4 mode ('landscape', 'mm', 'a4')
   - First page: title "${teamName} - Analytics Report", subtitle "Range: ${range} | Generated: ${new Date().toLocaleDateString()}"
   - For each chart element: `addPage()`, capture with `toPng(element, { backgroundColor: '#ffffff', pixelRatio: 1.5 })` (1.5 for PDF to reduce file size per research pitfall 3), add image to page with 10mm margins filling the page
   - Call `pdf.save(...)` with filename `${teamName}-analytics-${range}.pdf`

4. Create `apps/web/src/lib/export-csv.ts`:
   - Export function `downloadCsv(csvContent: string, filename: string): void` -- creates Blob with type 'text/csv;charset=utf-8;', creates object URL, triggers download via anchor click, revokes URL
   - Export function `escapeCsvField(value: string | number): string` -- wraps strings in double quotes, escapes internal quotes by doubling them
   - Export function `teamAnalyticsToCsv(data: TeamAnalyticsResponse): string` -- builds multi-section CSV:
     Section 1: "Submission Rates" header row, then Date,Rate (%),Submitted,Total for each submissionRates entry
     Section 2: blank line, "Stress Trend" header, then Date,Low,Medium,High for each stressTrend entry
     Section 3: blank line, "Task Volume by Member" header, then Member,Task Count for each taskVolumeByMember entry
     Section 4: blank line, "Workload Heatmap" header, then UserId,Member,Date,Hours for each heatmap entry
   - Export function `memberAnalyticsToCsv(data: MemberAnalyticsResponse): string` -- builds multi-section CSV:
     Section 1: "Daily Hours" header, Date,Hours,Team Avg
     Section 2: blank line, "Stress Timeline" header, Date,Level
     Section 3: blank line, "Daily Tasks" header, Date,Count
     Section 4: blank line, "Submission Calendar" header, Date,Submitted (yes/no)
   - Import types from '@daily-report/shared'
   - All string fields must be quoted with escapeCsvField to handle commas/quotes in member names

5. Create `apps/web/src/lib/__tests__/export-csv.test.ts`:
   - Import `teamAnalyticsToCsv`, `memberAnalyticsToCsv`, `escapeCsvField` from '../export-csv'
   - Test `escapeCsvField`:
     - wraps strings in double quotes
     - escapes internal double quotes by doubling them
     - passes numbers through as strings
   - Test `teamAnalyticsToCsv` with mock TeamAnalyticsResponse:
     - Create minimal mock with 2 submissionRates entries, 1 stressTrend entry, 1 taskVolumeByMember entry, 1 heatmap entry
     - Assert output contains "Submission Rates" section header
     - Assert output contains correct Date,Rate columns
     - Assert output contains "Stress Trend" section header
     - Assert output contains "Task Volume by Member" section header
     - Assert output contains "Workload Heatmap" section header
     - Assert sections are separated by blank lines
   - Test `memberAnalyticsToCsv` with mock MemberAnalyticsResponse:
     - Create minimal mock with 1 dailyHours, 1 stressTimeline, 1 dailyTasks, 1 submissionCalendar entry
     - Assert output contains all 4 section headers
     - Assert submission calendar shows "yes"/"no" for submitted field
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit && pnpm vitest run src/lib/__tests__/export-csv.test.ts</automated>
  </verify>
  <done>Three export utility files exist with correct TypeScript types, CSV unit tests pass, html-to-image and jspdf installed, tsc passes</done>
</task>

<task type="auto">
  <name>Task 2: Create ChartCard and ExportToolbar components</name>
  <files>apps/web/src/components/analytics/chart-card.tsx, apps/web/src/components/analytics/export-toolbar.tsx</files>
  <action>
1. Create `apps/web/src/components/analytics/chart-card.tsx`:
   - 'use client' directive
   - Props: `{ title: string; filename: string; children: React.ReactNode }`
   - Uses useRef<HTMLDivElement> on the outer Card element
   - Uses useState for `exporting` loading state
   - Renders Card with ref={cardRef}:
     - CardHeader with `className="flex flex-row items-center justify-between"` containing:
       - CardTitle with `className="text-base"` showing {title}
       - Button (variant="ghost", size="icon-sm") with Download icon from lucide-react (h-4 w-4), disabled={exporting}, title="Download as PNG", onClick calls exportPng(cardRef.current, filename) wrapped in try/finally that sets exporting
     - CardContent with {children}
   - Export named `ChartCard`

2. Create `apps/web/src/components/analytics/export-toolbar.tsx`:
   - 'use client' directive
   - Props interface `ExportToolbarProps`:
     - `getChartElements: () => HTMLDivElement[]` -- callback that returns current visible chart DOM elements for PDF
     - `teamName: string`
     - `range: string`
     - `onExportCsv: () => void` -- parent handles CSV because it has the data
   - Uses useState for `exportingPdf` loading state
   - Renders a div with `className="flex items-center gap-2"`:
     - Button (variant="outline", size="sm") with FileDown icon: "Export PDF", disabled={exportingPdf}, onClick calls `getChartElements()` to collect elements, then passes them to `exportAnalyticsPdf`
     - Button (variant="outline", size="sm") with FileSpreadsheet icon: "Export CSV", onClick calls onExportCsv
   - Import FileDown, FileSpreadsheet from lucide-react
   - Import exportAnalyticsPdf from '@/lib/export-pdf'
   - Export named `ExportToolbar`
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>ChartCard renders a Card with PNG download button, ExportToolbar provides PDF and CSV export buttons using getChartElements callback, tsc passes</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm exec tsc --noEmit` passes with no errors
- `cd apps/web && pnpm vitest run src/lib/__tests__/export-csv.test.ts` passes
- All 5 new source files exist and export their named functions/components
- html-to-image and jspdf are in apps/web/package.json dependencies
</verification>

<success_criteria>
- Export utility functions (PNG, PDF, CSV) are implemented with correct types
- CSV builder functions have passing unit tests with mock data
- ChartCard component wraps Card with a download button
- ExportToolbar provides PDF and CSV export triggers via getChartElements callback
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-export-analytics/10-01-SUMMARY.md`
</output>
