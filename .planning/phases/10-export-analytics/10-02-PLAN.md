---
phase: 10-export-analytics
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/src/components/analytics/team-overview.tsx
  - apps/web/src/components/analytics/member-analytics.tsx
  - apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03

must_haves:
  truths:
    - "Owner can click a download icon on any chart card to save it as a PNG"
    - "Owner can click Export PDF to download a multi-page PDF of all visible charts"
    - "Owner can click Export CSV to download raw analytics data as a CSV file"
  artifacts:
    - path: "apps/web/src/components/analytics/team-overview.tsx"
      provides: "Team overview charts wrapped in ChartCard with PNG download buttons"
      exports: ["TeamOverview"]
    - path: "apps/web/src/components/analytics/member-analytics.tsx"
      provides: "Member analytics charts wrapped in ChartCard with PNG download buttons"
      exports: ["MemberAnalytics"]
    - path: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      provides: "Analytics page with ExportToolbar for PDF and CSV download"
  key_links:
    - from: "apps/web/src/components/analytics/team-overview.tsx"
      to: "apps/web/src/components/analytics/chart-card.tsx"
      via: "import { ChartCard }"
      pattern: "ChartCard"
    - from: "apps/web/src/components/analytics/member-analytics.tsx"
      to: "apps/web/src/components/analytics/chart-card.tsx"
      via: "import { ChartCard }"
      pattern: "ChartCard"
    - from: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      to: "apps/web/src/components/analytics/export-toolbar.tsx"
      via: "import { ExportToolbar }"
      pattern: "ExportToolbar"
    - from: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      to: "apps/web/src/lib/export-csv.ts"
      via: "import { downloadCsv, teamAnalyticsToCsv, memberAnalyticsToCsv }"
      pattern: "downloadCsv"
---

<objective>
Wire ChartCard and ExportToolbar into the existing analytics dashboard, replacing raw Card usage with ChartCard for PNG download, and adding PDF/CSV export buttons to the page header.

Purpose: Completes all three EXPORT requirements by connecting the utilities from Plan 01 to the existing analytics UI.
Output: Fully functional export capabilities on the analytics dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-export-analytics/10-01-SUMMARY.md
@apps/web/src/components/analytics/team-overview.tsx
@apps/web/src/components/analytics/member-analytics.tsx
@apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx

<interfaces>
<!-- From Plan 01 artifacts -->
From apps/web/src/components/analytics/chart-card.tsx:
```typescript
export function ChartCard({ title, filename, children }: {
  title: string;
  filename: string;
  children: React.ReactNode;
}): JSX.Element;
// Renders Card with ref, CardHeader with title + download button, CardContent with children
// The outer Card div has a ref that can be collected for PDF export
```

From apps/web/src/components/analytics/export-toolbar.tsx:
```typescript
export function ExportToolbar({ chartRefs, teamName, range, onExportCsv }: {
  chartRefs: React.RefObject<HTMLDivElement | null>[];
  teamName: string;
  range: string;
  onExportCsv: () => void;
}): JSX.Element;
```

From apps/web/src/lib/export-csv.ts:
```typescript
export function downloadCsv(csvContent: string, filename: string): void;
export function teamAnalyticsToCsv(data: TeamAnalyticsResponse): string;
export function memberAnalyticsToCsv(data: MemberAnalyticsResponse): string;
```

From packages/shared/src/types/analytics.ts:
```typescript
export type AnalyticsRange = 'week' | 'month' | 'quarter';
export interface TeamAnalyticsResponse { ... }
export interface MemberAnalyticsResponse { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Card with ChartCard in team-overview.tsx and member-analytics.tsx</name>
  <files>apps/web/src/components/analytics/team-overview.tsx, apps/web/src/components/analytics/member-analytics.tsx</files>
  <action>
1. Modify `apps/web/src/components/analytics/team-overview.tsx`:
   - Import `ChartCard` from './chart-card'
   - Replace each of the 4 chart Card wrappers with ChartCard. The 4 charts are:
     a. "Submission Rate" line chart -> `<ChartCard title="Submission Rate" filename="submission-rate">`
     b. "Workload Heatmap" table -> `<ChartCard title="Workload Heatmap" filename="workload-heatmap">`
     c. "Stress Trend" area chart -> `<ChartCard title="Stress Trend" filename="stress-trend">`
     d. "Task Volume by Member" bar chart -> `<ChartCard title="Task Volume by Member" filename="task-volume">`
   - For each: remove the inner `<CardHeader><CardTitle>...</CardTitle></CardHeader>` and `<CardContent>` wrappers since ChartCard provides those. Move the chart content (ResponsiveContainer or table div) to be ChartCard's children.
   - Keep the loading skeleton and empty state Card elements as-is (they are not charts, no export needed).
   - Keep the SummaryCard row unchanged (summary cards are not charts).
   - Add `useImperativeHandle` or `forwardRef` to expose chart refs: Change the component to use `forwardRef` and expose an object with `chartRefs: React.RefObject<HTMLDivElement | null>[]`. Actually, simpler approach: the parent analytics page needs refs to the ChartCard divs for PDF export. Since ChartCard already has an internal ref, we need a way to collect them. Change approach:
     - Do NOT use forwardRef. Instead, accept an optional prop `chartRefs?: React.MutableRefObject<(HTMLDivElement | null)[]>` on TeamOverview.
     - Inside TeamOverview, create 4 individual refs with useRef<HTMLDivElement>(null), and in a useEffect, populate chartRefs.current with the 4 ref values.
     - BUT ChartCard uses its own internal ref. We need ChartCard to also accept an optional `cardRef` prop so the parent can collect it. **Update Plan 01's ChartCard**: ChartCard should accept an optional `cardRef?: React.Ref<HTMLDivElement>` prop and merge it with its internal ref (or just use the passed ref if provided, falling back to internal). Use React.useImperativeHandle or simply: if cardRef is passed, attach it to the Card div alongside the internal ref using a callback ref pattern.

   SIMPLER APPROACH (preferred): Modify ChartCard to accept an optional `ref` via forwardRef that exposes the card div. The internal ref is used for PNG export. Use a callback ref that sets both the internal ref and the forwarded ref. This way the parent can collect refs easily.

   SIMPLEST APPROACH (use this): Don't pass refs through ChartCard at all. In TeamOverview, wrap each ChartCard in a div with a ref. The PDF export will capture these wrapper divs. This avoids modifying ChartCard's API. Each chart section gets a `ref={el => { if (chartRefsArray) chartRefsArray.current[index] = el; }}`.

   FINAL APPROACH: TeamOverview accepts optional prop `chartRefsCollector?: React.MutableRefObject<HTMLDivElement[]>`. Create 4 refs locally. In a useEffect, if chartRefsCollector is provided, set `chartRefsCollector.current = [ref1.current, ref2.current, ...].filter(Boolean)`. Each ChartCard's parent div gets its own ref. Actually even simpler -- just wrap each ChartCard in a div with a ref callback.

   OK, let's use this clean pattern:
   - TeamOverview props add: `chartRefsCollector?: React.MutableRefObject<HTMLDivElement[]>`
   - Create `const chartRef1 = useRef<HTMLDivElement>(null)` through chartRef4
   - Wrap each ChartCard in `<div ref={chartRef1}>` etc.
   - In a useEffect with [chartRefsCollector], set `chartRefsCollector.current` to the array of non-null refs
   - Remove Card/CardHeader/CardTitle/CardContent imports that are no longer directly used (keep Card for skeleton/empty states if needed)

2. Modify `apps/web/src/components/analytics/member-analytics.tsx`:
   - Import `ChartCard` from './chart-card'
   - Replace 4 chart Cards with ChartCard:
     a. "Hours Worked" composed chart -> `<ChartCard title="Hours Worked" filename="hours-worked">`
     b. "Stress Timeline" line chart -> `<ChartCard title="Stress Timeline" filename="stress-timeline">`
     c. "Task Breakdown" bar chart -> `<ChartCard title="Task Breakdown" filename="task-breakdown">`
     d. "Submission Calendar" -> `<ChartCard title="Submission Calendar" filename="submission-calendar">`
   - Same pattern: remove inner CardHeader/CardTitle/CardContent, move chart content as ChartCard children.
   - Add `chartRefsCollector?: React.MutableRefObject<HTMLDivElement[]>` prop, same ref collection pattern as TeamOverview.
   - Keep the member selector and summary cards unchanged.
   - Card import still needed for summary cards (Submission Streak, Avg Hours, Stress Pattern, Task Output).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>All 8 chart cards across both tabs have PNG download buttons via ChartCard wrapper, chart refs are collectable by parent</done>
</task>

<task type="auto">
  <name>Task 2: Add ExportToolbar to analytics page with PDF and CSV export</name>
  <files>apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx</files>
  <action>
1. Modify `apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx`:
   - Import `ExportToolbar` from '@/components/analytics/export-toolbar'
   - Import `downloadCsv`, `teamAnalyticsToCsv`, `memberAnalyticsToCsv` from '@/lib/export-csv'
   - Import `useRef` from react
   - Import `TeamAnalyticsResponse`, `MemberAnalyticsResponse` from '@daily-report/shared'

   - Create `const teamChartRefs = useRef<HTMLDivElement[]>([])` and `const memberChartRefs = useRef<HTMLDivElement[]>([])`

   - Create a ref array for ExportToolbar: `const activeChartRefs` computed based on activeTab -- use `useMemo` to create RefObject-shaped wrappers, or just pass the right MutableRefObject based on tab.

   Actually simpler: ExportToolbar expects `chartRefs: React.RefObject<HTMLDivElement | null>[]`. Change ExportToolbar to accept `getChartElements: () => HTMLDivElement[]` instead of refs array. This is cleaner -- the callback returns the current chart elements from whichever tab is active.

   WAIT -- Plan 01 already defined ExportToolbar's props. Let's work with what Plan 01 created or adjust slightly. Since Plan 01 hasn't been executed yet and we're planning, let's note: ExportToolbar should accept `getChartElements: () => HTMLDivElement[]` instead of `chartRefs`. Update the ExportToolbar in Plan 01 accordingly? No -- Plan 01 is already written. We'll just adapt here.

   APPROACH: Pass chartRefs based on activeTab. Create wrapper refs:
   ```
   const chartRefs = activeTab === 'team' ?
     teamChartRefs.current.map(el => ({ current: el })) :
     memberChartRefs.current.map(el => ({ current: el }));
   ```

   But this creates new objects every render. Better: ExportToolbar's onClick handler should just read from the mutable ref at call time. Since ExportToolbar receives chartRefs and reads .current on click, we can pass stable ref objects. Let's create individual useRef for each slot (up to 4 per tab = 8 total), grouped into arrays.

   CLEANEST APPROACH: Modify ExportToolbar (it hasn't been built yet, Plan 01 will follow this plan's spec) to accept `getChartElements: () => HTMLDivElement[]` instead of `chartRefs`. This is simpler. Update Plan 01's ExportToolbar spec: replace `chartRefs` prop with `getChartElements: () => HTMLDivElement[]`. The onClick handler calls `getChartElements()` to get the elements, then passes them to exportAnalyticsPdf.

   In the analytics page:
   - Define `const teamChartRefs = useRef<HTMLDivElement[]>([])`
   - Define `const memberChartRefs = useRef<HTMLDivElement[]>([])`
   - Pass `chartRefsCollector={teamChartRefs}` to TeamOverview
   - Pass `chartRefsCollector={memberChartRefs}` to MemberAnalytics
   - Define `getChartElements` callback: returns `activeTab === 'team' ? teamChartRefs.current : memberChartRefs.current`

   For CSV export, we need the analytics data. The data is fetched inside TeamOverview/MemberAnalytics, not in the parent page. Two options:
   a. Lift the data fetching to the page level (big refactor, avoid)
   b. Have TeamOverview/MemberAnalytics accept an `onDataLoaded` callback that passes data up
   c. Duplicate the useQuery in the page just for CSV (queries are cached by react-query, so no extra network request)

   Use option (c) -- it's simplest. Add useQuery calls in the analytics page for team analytics and member analytics data (same query keys = cache hit). Then the CSV handler checks activeTab and calls the appropriate CSV builder.

   For the member tab CSV, we also need the selected member's userId. Add a `selectedMemberId` state lifted to the page, or use option (b) for member data. Actually, the member analytics query needs a userId. Let's add state:
   - `const [selectedMemberId, setSelectedMemberId] = useState<string | null>(null)`
   - Pass `selectedMemberId` and `onMemberChange={setSelectedMemberId}` to MemberAnalytics
   - MemberAnalytics calls `onMemberChange` when member selection changes (in addition to its internal state)
   - Use the selectedMemberId in a duplicate useQuery for MemberAnalyticsResponse

   Alternatively, just have MemberAnalytics expose data via a callback `onDataReady?: (data: MemberAnalyticsResponse) => void` called in a useEffect when analytics data loads. Simpler, no query duplication.

   FINAL DESIGN:
   - TeamOverview gets prop `onDataReady?: (data: TeamAnalyticsResponse) => void` -- calls it in useEffect when data loads
   - MemberAnalytics gets prop `onDataReady?: (data: MemberAnalyticsResponse) => void` -- calls it in useEffect when analytics loads
   - Analytics page stores `teamData` and `memberData` in state via these callbacks
   - CSV handler: if activeTab === 'team' && teamData, call `downloadCsv(teamAnalyticsToCsv(teamData), '${teamName}-team-analytics-${range}')`; if activeTab === 'member' && memberData, call `downloadCsv(memberAnalyticsToCsv(memberData), '${teamName}-member-analytics-${range}')`

   Implementation:
   - Add ExportToolbar between the tab toggle and tab content, aligned right, in the header area next to the time range toggle. Place it in the header row: wrap the range toggle and ExportToolbar in a flex container.
   - The ExportToolbar renders: "Export PDF" button and "Export CSV" button
   - Pass `getChartElements`, `teamName`, `range`, and `onExportCsv` to ExportToolbar

   NOTE FOR EXECUTOR: Plan 01's ExportToolbar should use `getChartElements: () => HTMLDivElement[]` prop instead of `chartRefs`. The executor should implement ExportToolbar with this interface since both plans execute sequentially.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Analytics page has Export PDF and Export CSV buttons in the header. Each chart card has a PNG download icon. PNG downloads individual chart as image. PDF downloads all visible charts as multi-page PDF. CSV downloads raw analytics data for the active tab.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm exec tsc --noEmit` passes
- `pnpm test` passes (no backend changes, existing tests unaffected)
- Manual: Visit /manager/{teamId}/analytics, verify download icon appears on each chart card
- Manual: Click a chart download icon, verify PNG file is saved
- Manual: Click Export PDF, verify multi-page PDF is generated with all charts
- Manual: Click Export CSV on Team Overview tab, verify CSV with submission rates, stress trend, task volume, heatmap data
- Manual: Click Export CSV on Individual Member tab, verify CSV with daily hours, stress timeline, tasks, submission calendar
</verification>

<success_criteria>
- All 8 chart cards (4 team + 4 member) have working PNG download buttons (EXPORT-01)
- Export PDF button generates a landscape PDF with title page and one chart per page (EXPORT-02)
- Export CSV button downloads properly formatted CSV data for the active tab (EXPORT-03)
- No regressions in existing analytics functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-export-analytics/10-02-SUMMARY.md`
</output>
