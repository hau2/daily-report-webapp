---
phase: 10-export-analytics
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/src/components/analytics/team-overview.tsx
  - apps/web/src/components/analytics/member-analytics.tsx
  - apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
  - EXPORT-03

must_haves:
  truths:
    - "Owner can click a download icon on any chart card to save it as a PNG"
    - "Owner can click Export PDF to download a multi-page PDF of all visible charts"
    - "Owner can click Export CSV to download raw analytics data as a CSV file"
  artifacts:
    - path: "apps/web/src/components/analytics/team-overview.tsx"
      provides: "Team overview charts wrapped in ChartCard with PNG download buttons"
      exports: ["TeamOverview"]
    - path: "apps/web/src/components/analytics/member-analytics.tsx"
      provides: "Member analytics charts wrapped in ChartCard with PNG download buttons"
      exports: ["MemberAnalytics"]
    - path: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      provides: "Analytics page with ExportToolbar for PDF and CSV download"
  key_links:
    - from: "apps/web/src/components/analytics/team-overview.tsx"
      to: "apps/web/src/components/analytics/chart-card.tsx"
      via: "import { ChartCard }"
      pattern: "ChartCard"
    - from: "apps/web/src/components/analytics/member-analytics.tsx"
      to: "apps/web/src/components/analytics/chart-card.tsx"
      via: "import { ChartCard }"
      pattern: "ChartCard"
    - from: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      to: "apps/web/src/components/analytics/export-toolbar.tsx"
      via: "import { ExportToolbar }"
      pattern: "ExportToolbar"
    - from: "apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx"
      to: "apps/web/src/lib/export-csv.ts"
      via: "import { downloadCsv, teamAnalyticsToCsv, memberAnalyticsToCsv }"
      pattern: "downloadCsv"
---

<objective>
Wire ChartCard and ExportToolbar into the existing analytics dashboard, replacing raw Card usage with ChartCard for PNG download, and adding PDF/CSV export buttons to the page header.

Purpose: Completes all three EXPORT requirements by connecting the utilities from Plan 01 to the existing analytics UI.
Output: Fully functional export capabilities on the analytics dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-export-analytics/10-01-SUMMARY.md
@apps/web/src/components/analytics/team-overview.tsx
@apps/web/src/components/analytics/member-analytics.tsx
@apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx

<interfaces>
<!-- From Plan 01 artifacts -->
From apps/web/src/components/analytics/chart-card.tsx:
```typescript
export function ChartCard({ title, filename, children }: {
  title: string;
  filename: string;
  children: React.ReactNode;
}): JSX.Element;
// Renders Card with ref, CardHeader with title + download button, CardContent with children
```

From apps/web/src/components/analytics/export-toolbar.tsx:
```typescript
export function ExportToolbar({ getChartElements, teamName, range, onExportCsv }: {
  getChartElements: () => HTMLDivElement[];
  teamName: string;
  range: string;
  onExportCsv: () => void;
}): JSX.Element;
```

From apps/web/src/lib/export-csv.ts:
```typescript
export function downloadCsv(csvContent: string, filename: string): void;
export function teamAnalyticsToCsv(data: TeamAnalyticsResponse): string;
export function memberAnalyticsToCsv(data: MemberAnalyticsResponse): string;
```

From packages/shared/src/types/analytics.ts:
```typescript
export type AnalyticsRange = 'week' | 'month' | 'quarter';
export interface TeamAnalyticsResponse { ... }
export interface MemberAnalyticsResponse { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Card with ChartCard in team-overview.tsx and member-analytics.tsx</name>
  <files>apps/web/src/components/analytics/team-overview.tsx, apps/web/src/components/analytics/member-analytics.tsx</files>
  <action>
Use the "wrapper div with ref" pattern to collect chart elements for PDF export. Each component accepts a `chartRefsCollector` prop and wraps each ChartCard in a div with a ref, then populates the collector in a useEffect.

1. Modify `apps/web/src/components/analytics/team-overview.tsx`:
   - Add to props: `chartRefsCollector?: React.MutableRefObject<HTMLDivElement[]>`
   - Import `ChartCard` from './chart-card', add `useRef` and `useEffect` to react imports
   - Create 4 refs: `const chartRef1 = useRef<HTMLDivElement>(null)` through `chartRef4`
   - Add `useEffect(() => { if (chartRefsCollector) chartRefsCollector.current = [chartRef1, chartRef2, chartRef3, chartRef4].map(r => r.current).filter(Boolean) as HTMLDivElement[]; }, [chartRefsCollector]);`
   - Replace the 4 chart Card wrappers (NOT the loading skeletons or summary cards) with ChartCard wrapped in a div with ref:
     a. `<div ref={chartRef1}><ChartCard title="Submission Rate" filename="submission-rate">` -- move the ResponsiveContainer (or empty-state p tag) as children, remove inner CardHeader/CardTitle/CardContent
     b. `<div ref={chartRef2}><ChartCard title="Workload Heatmap" filename="workload-heatmap">` -- move the overflow-x-auto table div + legend as children
     c. `<div ref={chartRef3}><ChartCard title="Stress Trend" filename="stress-trend">` -- move the ResponsiveContainer as children
     d. `<div ref={chartRef4}><ChartCard title="Task Volume by Member" filename="task-volume">` -- move the ResponsiveContainer as children
   - Keep loading skeleton Cards and SummaryCard row unchanged
   - Add `onDataReady?: (data: TeamAnalyticsResponse) => void` prop. In a useEffect watching `data`, call `onDataReady(data)` when data is truthy.

2. Modify `apps/web/src/components/analytics/member-analytics.tsx`:
   - Add to props: `chartRefsCollector?: React.MutableRefObject<HTMLDivElement[]>`, `onDataReady?: (data: MemberAnalyticsResponse) => void`
   - Import `ChartCard` from './chart-card', add `useRef` and `useEffect` to react imports
   - Create 4 refs: `const chartRef1 = useRef<HTMLDivElement>(null)` through `chartRef4`
   - Same useEffect pattern as TeamOverview to populate chartRefsCollector
   - Replace 4 chart Cards with ChartCard wrapped in div with ref:
     a. `<div ref={chartRef1}><ChartCard title="Hours Worked" filename="hours-worked">` -- move ComposedChart ResponsiveContainer as children
     b. `<div ref={chartRef2}><ChartCard title="Stress Timeline" filename="stress-timeline">` -- move LineChart ResponsiveContainer as children
     c. `<div ref={chartRef3}><ChartCard title="Task Breakdown" filename="task-breakdown">` -- move BarChart ResponsiveContainer as children
     d. `<div ref={chartRef4}><ChartCard title="Submission Calendar" filename="submission-calendar">` -- move SubmissionCalendar as children
   - Keep member selector and 4 summary Cards unchanged (those are summary cards, not charts)
   - In a useEffect watching `analytics`, call `onDataReady(analytics)` when analytics is truthy
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>All 8 chart cards across both tabs have PNG download buttons via ChartCard wrapper, chart refs are collectable by parent via chartRefsCollector prop, analytics data is surfaced via onDataReady callback</done>
</task>

<task type="auto">
  <name>Task 2: Add ExportToolbar to analytics page with PDF and CSV export</name>
  <files>apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx</files>
  <action>
1. Modify `apps/web/src/app/(dashboard)/manager/[teamId]/analytics/page.tsx`:
   - Add imports:
     - `ExportToolbar` from '@/components/analytics/export-toolbar'
     - `downloadCsv`, `teamAnalyticsToCsv`, `memberAnalyticsToCsv` from '@/lib/export-csv'
     - `useRef`, `useState`, `useCallback` from react
     - `TeamAnalyticsResponse`, `MemberAnalyticsResponse` from '@daily-report/shared'

   - Add state and refs:
     - `const teamChartRefs = useRef<HTMLDivElement[]>([])`
     - `const memberChartRefs = useRef<HTMLDivElement[]>([])`
     - `const [teamData, setTeamData] = useState<TeamAnalyticsResponse | null>(null)`
     - `const [memberData, setMemberData] = useState<MemberAnalyticsResponse | null>(null)`

   - Create `getChartElements` callback (useCallback, deps: [activeTab or equivalent tab state]):
     - Returns `activeTab === 'team' ? teamChartRefs.current : memberChartRefs.current`
     - (Use the actual tab state variable name from the existing page code)

   - Create `handleExportCsv` callback:
     - If activeTab is 'team' and teamData exists: `downloadCsv(teamAnalyticsToCsv(teamData), '${teamName}-team-analytics-${range}')`
     - If activeTab is 'member' and memberData exists: `downloadCsv(memberAnalyticsToCsv(memberData), '${memberDisplayName}-analytics-${range}')`
     - (Use actual team name from the page's existing data; for member name use memberData.displayName)

   - Pass new props to TeamOverview: `chartRefsCollector={teamChartRefs}`, `onDataReady={setTeamData}`
   - Pass new props to MemberAnalytics: `chartRefsCollector={memberChartRefs}`, `onDataReady={setMemberData}`

   - Add ExportToolbar in the page header area, next to the time range toggle, aligned right. Wrap the existing range toggle and ExportToolbar in a flex container with justify-between if not already:
     ```
     <ExportToolbar
       getChartElements={getChartElements}
       teamName={teamName}
       range={range}
       onExportCsv={handleExportCsv}
     />
     ```
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/daily-report-webapp/apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Analytics page has Export PDF and Export CSV buttons in the header. Each chart card has a PNG download icon. PNG downloads individual chart as image. PDF downloads all visible charts as multi-page PDF. CSV downloads raw analytics data for the active tab.</done>
</task>

</tasks>

<verification>
- `cd apps/web && pnpm exec tsc --noEmit` passes
- `pnpm test` passes (no backend changes, existing tests unaffected)
- Manual: Visit /manager/{teamId}/analytics, verify download icon appears on each chart card
- Manual: Click a chart download icon, verify PNG file is saved
- Manual: Click Export PDF, verify multi-page PDF is generated with all charts
- Manual: Click Export CSV on Team Overview tab, verify CSV with submission rates, stress trend, task volume, heatmap data
- Manual: Click Export CSV on Individual Member tab, verify CSV with daily hours, stress timeline, tasks, submission calendar
</verification>

<success_criteria>
- All 8 chart cards (4 team + 4 member) have working PNG download buttons (EXPORT-01)
- Export PDF button generates a landscape PDF with title page and one chart per page (EXPORT-02)
- Export CSV button downloads properly formatted CSV data for the active tab (EXPORT-03)
- No regressions in existing analytics functionality
</success_criteria>

<output>
After completion, create `.planning/phases/10-export-analytics/10-02-SUMMARY.md`
</output>
