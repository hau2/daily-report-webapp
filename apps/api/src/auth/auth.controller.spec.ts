import { describe, it } from 'vitest';

describe('AuthController', () => {
  it.todo('cookie - login sets httpOnly access + refresh cookies');
  it.todo('refresh - issues new access token with valid refresh token');
  it.todo('expired - expired access token triggers refresh');
});
